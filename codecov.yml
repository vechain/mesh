coverage:
  status:
    patch:
      default:
        only_pulls: true
        target: auto
        threshold: 8%

# Comments on PRs
comment:
  layout: "header, diff, files"
  behavior: default
  require_changes: false

